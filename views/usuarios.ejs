<%- include('template/top', {tituloWeb: 'Servicios' }) %>

    <div class="container">
        Usuarios
        <div class="table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">First</th>
                        <th scope="col">Email</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (arrayUsuarios.length> 0) { %>
                        <% arrayUsuarios.forEach(element=> { %>
                            <tr>
                                <th scope="row">
                                    <%= element.id%>
                                </th>
                                <td>
                                    <%= element.name%>
                                </td>
                                <td>
                                    <%= element.email%>
                                </td>

                            </tr>
                            <% }) %>
                                <% } %>
                </tbody>
            </table>
        </div>
    </div>

    <%- include('template/footer')%>